package components

templ Typography(variant,label,styles string) {
	if variant == "header" {
		<h1 class={ "text-white", "font-sans", "text-5xl", styles }>{ label }</h1>
	} else {
		<p class={ "text-black font-sans text-sm", styles }>{ label }</p>
	}
}
